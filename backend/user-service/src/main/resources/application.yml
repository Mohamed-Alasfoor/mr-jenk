server:
  port: 8081
  ssl:
    enabled: ${SERVER_SSL_ENABLED:false}
    key-store: ${SERVER_SSL_KEY_STORE:}
    key-store-password: ${SERVER_SSL_KEY_STORE_PASSWORD:}
    key-store-type: ${SERVER_SSL_KEY_STORE_TYPE:PKCS12}
    key-alias: ${SERVER_SSL_KEY_ALIAS:buy01-dev}

spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${USER_SERVICE_MONGODB_URI:mongodb://localhost:27017/user_service_db}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}
  instance:
    secure-port-enabled: ${SERVER_SSL_ENABLED:false}
    non-secure-port-enabled: ${EUREKA_NON_SECURE_PORT_ENABLED:true}

management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  jwt:
    secret: ${JWT_SECRET:changeit-changeit-changeit-changeit-changeit-1234567890}
    expiration: ${JWT_EXPIRATION_MS:3600000}

app:
  services:
    media:
      base-url: ${MEDIA_SERVICE_INTERNAL_BASE_URL:http://localhost:8083}
      public-base-url: ${MEDIA_PUBLIC_BASE_URL:http://localhost:8080/media/images}
  admin:
    bootstrap:
      email: ${ADMIN_EMAIL:}
      password: ${ADMIN_PASSWORD:}
      full-name: ${ADMIN_FULL_NAME:Marketplace Admin}
